# AGENTS.md — 과제 8 제출용 패스키 포트폴리오 작업 기준

이 저장소는 기존 공개 소개 페이지를 유지하면서, 새로 만든 **비공개 영역만 패스키(WebAuthn)로 보호**하는 과제 8 제출물을 만든다.

## 0. 작업 우선순위

1. 과제 1 공개 소개 페이지를 훼손하지 않는다.
2. 비공개 자료는 인증 전 네트워크 응답에 포함하지 않는다.
3. 비밀번호를 만들거나 저장하지 않는다.
4. 서버가 challenge를 만들고 검증한다.
5. 서버는 패스키 공개키만 저장하고 개인키는 받지 않는다.
6. 사용자를 요청 파라미터가 아니라 인증된 세션 기준으로 식별한다.
7. 모든 통과/거절 증거를 실제 HTTP 요청으로 남긴다.
8. 실제 개인정보·비밀값·서비스 키를 제출물에 넣지 않는다.

## 1. 완주 체크리스트

### 제출 URL / 공개 첫 화면
- [ ] T08-C01 결과물 URL은 HTTPS다.
- [ ] T08-C02 소스 URL은 HTTPS다.
- [ ] T08-C03 결과물·소스 URL은 새 시크릿 창에서 계정 생성/로그인/초대/비밀번호/OAuth/CAPTCHA 없이 열린다.
- [ ] T08-C10 결과물 첫 화면은 공개 소개 페이지다.
- [ ] T08-C11 과제 1 공개 내용이 그대로 유지된다.
- [ ] T08-C12 실제 개인정보가 없고, 비공개 데이터는 만들어 넣은 예시 데이터라고 명시한다.

### 공개/비공개 화면 분리
- [ ] T08-C13 공개 영역과 비공개 영역이 화면에서 명확히 구분된다.
- [ ] T08-C14 비공개 항목이 3개 이상이다.
- [ ] T08-C15 인증 전에는 비공개 실제 내용이 화면에 보이지 않는다.
- [ ] T08-C16 인증 전 비공개 자료 직접 요청은 거절된다.
- [ ] T08-C17 거절 상태는 401 또는 403이다.
- [ ] T08-C18 인증 전 HTML/JSON/API 응답 본문에 비공개 내용이 없다.

### 패스키 등록
- [ ] T08-C19 서버가 등록용 challenge를 만든다.
- [ ] T08-C20 등록마다 challenge가 달라진다.
- [ ] T08-C21 등록 성공 후 DB에 공개키가 저장된다.
- [ ] T08-C22 저장 값이 공개키이며 비밀번호가 아니라는 설명이 제출문에 있다.
- [ ] T08-C23 등록 요청 본문에 개인키가 없다.
- [ ] T08-C24 패스키에 사람이 읽을 수 있는 이름이 저장된다.
- [ ] T08-C25 등록을 취소하면 사용자/패스키 DB 레코드가 생성되지 않고 준비용 challenge도 정리된다.
- [ ] T08-C26 패스키가 실제로 저장된 위치(예: 기기 자체/Google Password Manager/보안 키)를 제출문에 적는다.

### 패스키 로그인
- [ ] T08-C27 로그인 때마다 새 challenge가 생성된다.
- [ ] T08-C28 로그인 요청마다 challenge가 다르다는 기록을 남긴다.
- [ ] T08-C29 서버가 저장된 공개키로 서명을 검증한다.
- [ ] T08-C30 정상 서명 성공과 잘못된 서명 실패를 나란히 기록한다.
- [ ] T08-C31 이미 성공에 사용한 challenge를 재전송하면 거절된다.
- [ ] T08-C32 로그인 후 사람을 구분하는 세션/토큰 방식을 설명한다.
- [ ] T08-C33 로그아웃 뒤 보호 자료 접근이 거절된다.
- [ ] T08-C34 세션/토큰 값은 제출 증거에서 마스킹한다.
- [ ] T08-C35 비밀번호 입력 칸이 없다.

### 사용자 격리
- [ ] T08-C36 계정 A/B 두 개가 있고 각각 다른 비공개 내용이 있다.
- [ ] T08-C37 A 인증 상태에서 B 자료 접근이 거절된다.
- [ ] T08-C38 B 인증 상태에서 A 자료 접근이 거절된다.
- [ ] T08-C39 거절 전후 상대 자료의 건수가 변하지 않는다.
- [ ] T08-C40 URL/body에 다른 user_id를 넣어도 타인 자료가 반환되지 않는다.
- [ ] T08-C41 위 권한 거절을 만드는 소스 위치를 제출문에 적는다.

### 패스키 2개 / 삭제
- [ ] T08-C42 한 계정에 패스키 2개를 등록한다.
- [ ] T08-C43 목록에 각 패스키 이름과 등록 날짜가 보인다.
- [ ] T08-C44 하나를 삭제한 후 남은 패스키로 로그인 성공한다.
- [ ] T08-C45 삭제한 패스키는 로그인에 실패한다.
- [ ] T08-C46 패스키 0개일 때 동작을 화면과 제출문에 적는다.

### 제출 설명서
- [ ] T08-C47 다음 6개 항목을 별도 제목으로 작성한다: ① 무엇으로 붙였나 ② 왜 골랐나 ③ 어디를 어떻게 고쳤나 ④ 안 열리는 것 확인 기록 ⑤ AI와 나 ⑥ 아직 못 막은 것.
- [ ] T08-C48 ①에 직접 구현/라이브러리/인증 서비스와 기술 이름을 적는다.
- [ ] T08-C49 ③에 등록/로그인/로그아웃/비공개 조회의 소스 위치를 적는다.
- [ ] T08-C50 ④에 로그인 없이 열기/남의 패스키로 열기/challenge 재사용/패스키 삭제 뒤 로그인의 성공·거절 기록을 모두 넣는다.
- [ ] T08-C51 ⑥에 아직 못 막은 보안 항목을 최소 1개 구체적으로 적는다.
- [ ] T08-C52 짧은 확인 방법 4줄을 작성한다: 어디로 가나 / 세 단계 안에 무엇을 하나 / 무엇이 보이면 통과인가 / 안 될 때 무엇이 보이나.
- [ ] T08-C53 AI에게 맡긴 일 / 내가 직접 판단한 일 / AI 제안을 따르지 않은 일을 각각 작성한다. 없으면 이유를 쓴다.

## 2. 화면 설계 기준

### 공개 첫 화면
기존 포트폴리오의 Hero → What I Did → What I Like 순서를 유지한다.

### 비공개 영역
페이지 맨 아래 별도 섹션을 사용한다.

```text
PRIVATE / 04
나만의 자리
공개 포트폴리오와 분리한 개인 작업 기록

[ 인증 전 ]
패스키로 잠금 해제
실제 본문은 네트워크 응답에 없음

잠긴 항목
- 프로젝트 메모
- 지원 목록
- 개인 회고

[ 패스키로 들어가기 ]

[ 첫 패스키 등록 ]  ← 최초 계정 준비용
[ 패스키로 들어가기 ]
인증 후: [ 새 패스키 등록 ]  ← 같은 계정에 추가
```

인증 후에는 실제 3개 카드와 패스키 관리 목록을 보여준다.

## 3. 금지 사항

- 비밀번호 입력/저장 금지
- OAuth 로그인 추가 금지
- CSS `display:none` 등 화면 숨김만으로 비공개 처리 금지
- private 데이터 하드코딩을 React 번들에 넣지 않는다.
- service role key를 `VITE_*` 환경변수로 넣지 않는다.
- 실제 연락처/주민번호/계좌번호/토큰/비밀키를 저장하거나 제출하지 않는다.
- 제출 증거에 쿠키·세션·토큰의 실제 값을 찍지 않는다.

## 4. 현재 구현 구조

```text
React/Vite
  ↓
Supabase Edge Function: passkey-api
  ├─ enroll-start
  ├─ enroll-cancel
  ├─ register-options
  ├─ register-verify
  ├─ auth-options
  ├─ auth-verify
  ├─ me
  ├─ private-items
  ├─ private-for-user
  ├─ passkeys
  ├─ passkey-delete
  └─ logout
  ↓
Supabase PostgreSQL
  ├─ portfolio_users
  ├─ passkeys
  ├─ webauthn_challenges
  └─ private_items
```

## 5. 인증 핵심 원칙

### 등록
- 서버가 랜덤 challenge 생성
- DB에 challenge와 만료시간 저장
- 브라우저가 WebAuthn credential 생성
- 브라우저가 credential ID + 공개키 기반 attestation 응답을 서버로 전달
- 서버가 `verifyRegistrationResponse`로 검증
- 서버에 public_key 저장
- private key는 서버 코드/DB/네트워크 어디에도 저장하지 않음

### 로그인
- 매 요청마다 새 challenge 생성
- challenge는 5분 이내만 유효
- 검증 성공 전에 challenge를 소비하여 재사용 방지
- 저장된 credential의 public key로 서명 검증
- 성공 시 HMAC 기반 HttpOnly 세션 쿠키 발급
- 로그아웃 시 세션 쿠키 제거

### 권한
비공개 자료는 항상 `authenticated session → portfolio_users.id → private_items.user_id` 경로로 조회한다.

`?user_id=다른사람` 같은 요청을 받아도 현재 세션의 사용자와 다르면 403을 반환한다.

## 6. DB 보안

모든 관련 테이블은 RLS를 켜고 브라우저 직접 접근 권한을 주지 않는다.
서비스 역할은 Edge Function 안에서만 사용한다.

## 7. 증거를 만들 때의 원칙

실제 비밀번호/비밀값/쿠키값을 캡처하지 않는다.

예:

```text
Cookie: tb_portfolio_session=***MASKED***
HTTP 401
{"error":"authentication_required"}
```

Challenge 값은 필요하면 앞 8자만 남기고 나머지를 마스킹한다.

```text
challenge #1 = 8d41c9a2********
challenge #2 = 7f28b1c0********
=> 서로 다름
```

## 8. 아직 못 막은 것 작성 원칙

과장하지 않는다. 실제 구현 상태에 맞춰 최소 1개를 적는다.
예:
- 세션 무효화를 DB의 별도 session-revocation 테이블까지 확장하지 않았다.
- 계정 삭제/복구 기능은 구현하지 않았다.
- challenge 정리용 주기적 purge 작업은 추가하지 않았다.

## 9. 완료 후 반드시 실행할 것

```text
npm install
npm run build
```

그 다음 실제 HTTPS 배포 환경에서 다음을 확인한다.

1. 새 시크릿 창 → 공개 첫 화면 정상
2. 비공개 API 직접 호출 → 401/403
3. 계정 A/B 생성
4. A/B 각각 비공개 3개 확인
5. challenge #1/#2가 다름 확인
6. 정상 로그인 성공
7. 잘못된 서명 401 확인
8. 같은 challenge 재사용 401 확인
9. A→B, B→A 모두 403 확인
10. user_id 변조 요청에도 타인 데이터 미반환
11. 패스키 2개 등록
12. 하나 삭제 후 남은 패스키 로그인 성공
13. 삭제한 패스키 로그인 실패
14. 마지막 패스키 삭제 후 자동 로그아웃
15. 로그아웃 후 private-items 401 확인
