# 과제 8 패스키 — 남은 작업 체크리스트

작성 기준: 2026-09-07

## 이미 완료한 로컬 작업

- [x] 패스키 등록·로그인 API 계약 통일
  - `authChallengeToken`
  - `X-Auth-Challenge-Token`
  - 서명된 `registrationToken`
  - `X-Registration-Token`
- [x] 등록 token의 5분 만료와 HMAC 서명 검증
- [x] challenge 1회 소비 및 등록 취소 시 미사용 challenge 정리
- [x] 계정별 서로 다른 가상 비공개 자료 3개 생성
- [x] 패스키 삭제 query parameter 조립 수정
- [x] 패스키 삭제 후 마지막 세션 폐기 처리
- [x] 네트워크 오류·브라우저 취소·만료 challenge·삭제 패스키 오류 메시지 분리
- [x] `.env.example`에 placeholder만 저장
- [x] 제출 설명서 6개 항목과 세션 방식 정리
- [x] `npm run lint` 통과
- [x] `npm run build` 통과
- [x] build 결과에 비공개 카드 본문과 service role key가 없는 것 확인

## 내일 먼저 할 일 — 배포 준비

- [ ] 로컬 `.env`가 저장되어 있는지 확인
- [ ] `VITE_SUPABASE_URL` 확인
- [ ] `VITE_SUPABASE_ANON_KEY` 또는 `VITE_SUPABASE_PUBLISHABLE_KEY` 확인
- [ ] service role key를 프런트 `.env`에 넣지 않았는지 확인
- [ ] Vercel에 프런트 환경변수 추가
- [ ] Supabase Edge Function `passkey-api`가 최신 코드인지 확인
- [ ] Edge Function secret 설정
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `WEBAUTHN_RP_ID`
  - `WEBAUTHN_ORIGIN`
- [ ] 배포 도메인과 `WEBAUTHN_RP_ID` host가 정확히 일치하는지 확인
- [ ] `WEBAUTHN_ORIGIN`이 `https://배포도메인`과 정확히 일치하는지 확인
- [ ] 배포 후 실제 HTTPS 주소 저장

## 공개 화면 확인

- [ ] 시크릿 창에서 배포 URL이 로그인 없이 열림
- [ ] 첫 화면이 기존 공개 포트폴리오임
- [ ] Hero → What I Did → What I Like 순서 유지
- [ ] 하단 `PRIVATE / 04`가 공개 영역과 분리됨
- [ ] 실제 개인정보가 없음

## 비공개 경계 확인

- [ ] 인증 전에는 비공개 카드 본문이 보이지 않음
- [ ] 인증 없이 `private-items` 요청 → `401`
- [ ] 인증 없이 `me` 요청 → `401`
- [ ] 인증 없이 `passkeys` 요청 → `401`
- [ ] 비로그인 HTML·JS·API 응답에 비공개 본문이 없음
- [ ] 브라우저 Network 캡처에서 token·credential ID·challenge 마스킹

## 모바일 패스키 확인

- [ ] Android Chrome 또는 iPhone Safari에서 배포 HTTPS URL 접속
- [ ] `PRIVATE / 04`에서 `첫 패스키 등록` 선택
- [ ] 테스트 계정 A 생성
- [ ] 휴대폰 패스키 이름 입력
- [ ] 지문·Face ID·PIN 승인
- [ ] 패스키 저장 위치 기록
  - Android: Google Password Manager 또는 기기 저장소
  - iPhone: iCloud Keychain 또는 기기 저장소
- [ ] 같은 휴대폰에서 패스키 로그인 성공
- [ ] 인증 후 비공개 카드 3개 표시
- [ ] 로그아웃 후 `private-items` → `401`

## 등록 검증

- [ ] 등록 2회 실행 후 challenge가 서로 다름
- [ ] 등록 요청 body에 개인키가 없음
- [ ] 서버 DB에 공개키만 저장됨
- [ ] 패스키 이름과 등록 날짜 표시
- [ ] 등록 창에서 취소
- [ ] 취소 후 새 사용자·패스키가 생성되지 않음
- [ ] 취소 후 미사용 registration challenge가 정리됨

## 로그인 검증

- [ ] 로그인 시도 2회의 challenge가 서로 다름
- [ ] 정상 서명 로그인 → `200`
- [ ] 잘못된 서명 → `401`
- [ ] 이미 사용한 challenge 재전송 → `401`
- [ ] 로그인 후 sessionStorage bearer token 방식 확인
- [ ] 로그아웃 후 같은 token으로 요청 → `401`
- [ ] 실제 token과 challenge 전체 값은 증거에서 마스킹

## A/B 사용자 격리

- [ ] 계정 A 등록
- [ ] 계정 B 등록
- [ ] A/B의 가상 비공개 자료가 서로 다름
- [ ] A 세션 → B `user_id` 요청 → `403`
- [ ] B 세션 → A `user_id` 요청 → `403`
- [ ] 거절 전후 상대 자료 개수가 변하지 않음
- [ ] URL/body의 `user_id` 변조로 타인 자료가 반환되지 않음

## 패스키 2개와 삭제

- [ ] 한 계정에 패스키 2개 등록
- [ ] 두 패스키의 이름과 등록 날짜 캡처
- [ ] 패스키 1 삭제
- [ ] 패스키 2 로그인 성공
- [ ] 삭제한 패스키 1 로그인 실패
- [ ] 마지막 패스키 삭제
- [ ] 자동 로그아웃 확인
- [ ] 기존 session token으로 보호 API 요청 → `401`
- [ ] 패스키 0개 화면과 복구 제약을 설명서에 기록

## 제출 설명서와 증거

- [ ] `docs-assignment8.md`의 배포 URL 입력
- [ ] 모바일 기기와 패스키 저장 위치 입력
- [ ] 실제 HTTP 상태와 응답을 `미실행`에서 실제 결과로 교체
- [ ] 성공·실패·재사용·삭제 후 로그인 결과를 나란히 기록
- [ ] 공개키 저장 확인 기록 추가
- [ ] A/B 자료 차이와 양방향 `403` 기록 추가
- [ ] 모든 세션·token·challenge·credential ID 마스킹
- [ ] 아직 못 막은 보안 항목을 실제 상태에 맞게 유지

## 최종 명령

```powershell
npm install
npm run lint
npm run build
```

## 주의

- [ ] GitHub push 전 `.env`, 실제 키, 개인키, 전체 token이 포함되지 않았는지 확인
- [ ] `SUPABASE_SERVICE_ROLE_KEY`가 프런트 bundle이나 GitHub에 없는지 확인
- [ ] DB 삭제·truncate·초기화 금지
- [ ] 실제 통과하지 않은 항목은 통과로 표시하지 않기
